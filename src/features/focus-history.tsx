import { StyleSheet, Text, View } from "react-native"
import { colors } from "../utils/colors"

export const FocusHistory = ({ history }) => {
  return (
    <View>
      <Text>a</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  historyWrapper: { color: colors.toDefine2 },
})
